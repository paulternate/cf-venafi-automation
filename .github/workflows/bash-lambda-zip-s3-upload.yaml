name: Zip and Upload Bash Lambdas to S3

on:
  push:
    branches:
      - main
    paths:
      - 'lambda/*'
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
    - name: Set up AWS CLI
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Zip and upload Bash Lambdas
      run: |
        for dir in lambda/*; do
          if [ -d ${dir} ]; then
            zip -jr ${dir}.zip ${dir}/app/
          fi
        done
        find . | grep zip
        
        # for file in lambda/*.py; do
        #   zip "${file%.*}.zip" "$file"
        #   aws s3 cp "${file%.*}.zip" s3://venafi-ecosystem/templates/
        # done
